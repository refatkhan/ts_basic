//reference type : object
//optional chaining
//literal types ? 
//readonly ? 
const user: {
    organization: string;
    firstName: string;
    middleName?: string;
    lastName: string;
} = {
    organization: "Siterois",
    firstName: "Ceo",
    lastName: "Next"
}
console.log(user)

